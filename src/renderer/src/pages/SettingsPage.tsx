import { Card } from 'antd';
import { PageHeader } from '../components/PageHeader';

export default function SettingsPage() {
  return (
    <div>
      <PageHeader title="设置" description="AI Provider 配置、API Key、数据备份与清空（T011 实现）" />
      <Card>设置表单将在 T011 实现。</Card>
    </div>
  );
}
